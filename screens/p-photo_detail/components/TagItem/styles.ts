import { StyleSheet } from 'react-native';


const styles = StyleSheet.create({
  tag: {
    backgroundColor: 'rgba(56, 189, 248, 0.2)', // accent with opacity
    color: '#38BDF8', // accent
    fontSize: 14,
    paddingHorizontal: 12,
    paddingVertical: 4,
    borderRadius: 16,
    marginRight: 8,
    marginBottom: 8,
    overflow: 'hidden',
  },
});

export default styles;